{
  "name": "tp10",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "runapitests": "cypress run --browser chrome --spec cypress/e22/API/**/*.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.3.0",
    "cypress-sql-server": "^1.0.0"
  }
}
